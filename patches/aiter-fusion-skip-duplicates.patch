diff --git a/vllm/compilation/passes/fusion/rocm_aiter_fusion.py b/vllm/compilation/passes/fusion/rocm_aiter_fusion.py
index 03d291d4d..d2765e05f 100644
--- a/vllm/compilation/passes/fusion/rocm_aiter_fusion.py
+++ b/vllm/compilation/passes/fusion/rocm_aiter_fusion.py
@@ -113,7 +113,7 @@ class AiterRMSNormDynamicQuantPattern(AiterRMSNormQuantPattern):
             # input, weight
             [self.empty(5, 16), self.empty(16)],
             pm.fwd_only,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -175,7 +175,7 @@ class AiterFusedAddRMSNormDynamicQuantPattern(AiterRMSNormQuantPattern):
             replacement,
             inputs,
             pm.fwd_only,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -231,7 +231,7 @@ class AiterRMSFp8GroupQuantPattern(AiterRMSNormQuantPattern):
             # input, weight
             [self.empty(5, 16), self.empty(16)],
             pm.fwd_only,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -294,7 +294,7 @@ class AiterFusedAddRMSFp8GroupQuantPattern(AiterRMSNormQuantPattern):
             self.empty(5, 16),  # residual
         ]
 
-        pm.register_replacement(pattern, replacement, inputs, pm.fwd_only, pm_pass)
+        pm.register_replacement(pattern, replacement, inputs, pm.fwd_only, pm_pass, skip_duplicates=True)
 
 
 class DoubleAiterRMSFp8GroupQuantPattern(AiterRMSNormQuantPattern):
@@ -362,7 +362,7 @@ class DoubleAiterRMSFp8GroupQuantPattern(AiterRMSNormQuantPattern):
             # input, weight
             [self.empty(5, 16), self.empty(16)],
             pm.fwd_only,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -448,7 +448,7 @@ class DoubleAiterRMSFp8GroupQuantViewPattern(AiterRMSNormQuantPattern):
             # input, weight
             [self.empty(5, 16), self.empty(16)],
             trace_with_view_to_reshape,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -539,7 +539,7 @@ class AiterRMSNormGatedFp8GroupQuantPattern(AiterRMSNormQuantPattern):
             replacement,
             [x, z, w],
             trace_fn,
-            pm_pass,
+            pm_pass, skip_duplicates=True,
         )
 
 
@@ -802,7 +802,7 @@ class AddAiterRMSNormPadPattern:
             return result_padded, residual_out, router_logits
 
         pm.register_replacement(
-            pattern, replacement, self.get_inputs(), pm.fwd_only, pm_pass
+            pattern, replacement, self.get_inputs(), pm.fwd_only, pm_pass, skip_duplicates=True
         )
 
 
